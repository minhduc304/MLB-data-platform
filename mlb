#!/usr/bin/env python3
"""
MLB Prop Prediction System CLI

Usage:
    ./mlb --help                    # Show all commands
    ./mlb collect --help            # Collection commands
    ./mlb player --help             # Player commands
    ./mlb team --help               # Team commands
    ./mlb scrape --help             # Props scraping commands
    ./mlb ml --help                 # ML pipeline commands

Examples:
    ./mlb collect init-db
    ./mlb collect teams
    ./mlb player update-all
    ./mlb scrape all
    ./mlb ml pipeline --dry-run
"""

import sys
import os

# Add project root to path
PROJECT_ROOT = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, PROJECT_ROOT)

from src.cli.main import cli

if __name__ == '__main__':
    cli()
